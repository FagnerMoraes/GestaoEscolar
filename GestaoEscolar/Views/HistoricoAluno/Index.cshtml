@model IEnumerable<GestaoEscolar.Models.HistoricoAluno>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutLista.cshtml";
}



<div class="row">
    <div class="col-md-8 col-md-offset-2">
        <p>
            <a href="@Url.Action("Adicionar", "HistoricoAluno")" class="btn btn-block btn-lg btn-success"><span class="fa fa-plus"></span><span>&nbsp;</span>Novo Histórico</a>
        </p>
        <div class="panel panel-success">

            <div class="panel-heading">
                <div class="row">
                    <div class="col-md-4">
                        <h4>Históricos Cadastrados</h4>
                    </div>
                    <div class="col-md-8">
                        @using (@Html.BeginForm("Index", "HistoricoAluno", FormMethod.Get))
                        {
                            <div class="input-group">
                                <input type="text" class="form-control" name="termoBusca" id="termoBusca" placeholder="Buscar por...">
                                <span class="input-group-btn">
                                    <button type="submit" class="btn btn-default">Buscar</button>
                                </span>
                            </div>


                        }
                    </div>
                </div>
            </div>
            <table class="table table-hover">
                <tr>
                    <th>
                        Nome Aluno
                    </th>
                    <th>
                        Ano do historico
                    </th>
                    <th>
                        Turma
                    </th>

                    <th>
                        AÇÕES
                    </th>
                </tr>

                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Aluno.Nome)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.AnoHistoricoAluno)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.TurmaAnoHistorico)
                        </td>

                        <td>
                            <a class="btn btn-default" title="Editar" href="@Url.Action("Editar", "HistoricoAluno", new {id = item.Id})">
                                <span class="fa fa-edit"></span>
                            </a>

                            <a class="btn btn-default" title="Detalhes" href="@Url.Action("Detalhes", "HistoricoAluno", new {id = item.Id})">
                                <span class="fa fa-list-alt"></span>
                            </a>

                            <a class="btn btn-primary btn-danger confirm" title="Excluir" href="@Url.Action("Excluir", "HistoricoAluno", new {id = item.Id})">
                                <span class="fa fa-trash"></span>
                            </a>

                        </td>
                    </tr>
                }

            </table>
        </div>
    </div>
</div>

<script src="~/Scripts/jquery.confirm.js"></script>
<script>
    $(".confirm").confirm({
        text: "Tem certeza que deseja excluir?",
        confirmButton: "Sim",
        cancelButton: "Não"
    });
</script>